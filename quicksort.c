#include "./includes/push_swap.h"

// int main(void)
// {
//     doubly *siko;
//     doubly *curr;

//     siko = (doubly *)malloc(sizeof(doubly));

//     siko->prev = NULL;
//     siko->num = 1;
//     siko->next = NULL;

//     curr = (doubly *)malloc(sizeof(doubly));
//     siko->next = curr;
//     curr->prev = siko;
//     curr->num = 2;
//     curr->next = NULL;

//     curr = (doubly *)malloc(sizeof(doubly));
//     curr->prev->next = curr;
//     curr



// }